<div class="row">
  <div class="phone-images">
    <%=image_tag(@product.photo.url,:class =>"phone") if @product.photo? %>
  </div>
  <h1><%= @product.name %></h1>
  <p><%= @product.description %></p>
</div>